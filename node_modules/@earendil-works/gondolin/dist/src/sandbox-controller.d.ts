import { EventEmitter } from "events";
export type SandboxConfig = {
    qemuPath: string;
    kernelPath: string;
    initrdPath: string;
    rootfsPath?: string;
    memory: string;
    cpus: number;
    virtioSocketPath: string;
    virtioFsSocketPath: string;
    append: string;
    machineType?: string;
    accel?: string;
    cpu?: string;
    console?: "stdio" | "none";
    netSocketPath?: string;
    netMac?: string;
    autoRestart: boolean;
};
export type SandboxState = "starting" | "running" | "stopped";
export declare class SandboxController extends EventEmitter {
    private readonly config;
    private child;
    private state;
    private restartTimer;
    private manualStop;
    constructor(config: SandboxConfig);
    setAppend(append: string): void;
    getState(): SandboxState;
    start(): Promise<void>;
    stop(): Promise<void>;
    restart(): Promise<void>;
    private scheduleRestart;
    private setState;
}
//# sourceMappingURL=sandbox-controller.d.ts.map